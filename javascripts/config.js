var require = {
    //这里使用的是config.js的目录
        paths: {
            vue: './lib/vue',
            Zepto: './lib/zepto.min',
            //helper
            req: './com/request',
            loading: './com/loading'
        },
        shim: {
        }
};