//做路由切换时需要改为define
require(['vue', 'Zepto','req', 'loading', 'Store', 'message'], function (Vue, $, Req, Loading, Store, Msg) {
    var Booking = new Vue({
        el: '#j_paywaiting',
        data: {
        },
        created: function () {

        },
        methods: {

        },
        computed: {

        }
    });
});
